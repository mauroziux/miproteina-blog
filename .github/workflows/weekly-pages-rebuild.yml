name: Weekly Pages Rebuild
on:
  schedule:
    - cron: '55 12 * * 1'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "weekly rebuild trigger test"
